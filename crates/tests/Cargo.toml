[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
macros = { version = "0.1.0", path = "../macros" }
shared = { version = "0.1.0", path = "../shared" }
tokio = { version = "1.28.2", features = ["full"] }